services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8085:80"
    volumes:
      - .:/var/www/html:cached
      - /var/www/html/vendor
    environment:
      CI_ENVIRONMENT: development
    restart: unless-stopped
